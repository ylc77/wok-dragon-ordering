# Wok Dragon Ordering 中文宣传演示视频脚本

目标时长：60-90 秒  
视频形式：Playwright 自动录屏 + 中文字幕浮层  
目标观众：餐馆老板、门店经理、潜在客户  
核心表达：客户能快速看懂这套系统能做什么，不强调技术细节。

## 分镜脚本

| 时间 | 页面 / 画面 | 录屏动作 | 中文字幕 |
| --- | --- | --- | --- |
| 0-6s | 首页 `/` | 打开首页，停留 Hero 和“查看菜单”按钮 | 餐馆官网、扫码点餐、后台管理和 POS 点单，一套系统完成。 |
| 6-12s | 首页第二屏 | 向下滚动，展示营业信息、地址、电话、卖点 | 首页展示品牌、营业信息和查看菜单入口，客户一打开就能看懂。 |
| 12-20s | 公开菜单 `/menu` | 打开公开菜单，滚动菜品列表和分类 | 公开菜单适合分享给顾客浏览，只展示菜品、价格、图片和售罄状态。 |
| 20-25s | 公开菜单多语言 | 尝试切换希腊语或展示多语言入口 | 前台支持英文和希腊语，方便本地顾客和游客使用。 |
| 25-34s | 扫码点餐 `/table/:qrToken` | 打开演示桌码，进入或加入桌台会话 | 顾客扫桌码进入点餐页，系统识别当前桌台和用餐会话。 |
| 34-43s | 点餐页菜品列表 | 选择一个菜品，若有口味选项则展示选择 | 顾客按分类浏览菜品，选择后可以加入购物车。 |
| 43-51s | 购物车 / 提交订单 | 加入购物车，打开购物车，提交订单 | 购物车按桌台会话隔离，同桌多人点餐也能保持一致。 |
| 51-60s | 后台登录 / 订单管理 | 登录 `/admin`，展示订单列表 | 顾客提交订单后，后台可以实时看到新订单。 |
| 60-66s | 订单管理 | 展示确认收款、清桌入口，不点击危险操作 | 确认收款和清桌入口清楚可见，演示时不需要点击危险操作。 |
| 66-72s | 桌台 / 二维码 | 切到桌台管理，展示二维码管理区域 | 桌台二维码可以下载、打印和重生成，旧二维码会失效。 |
| 72-80s | 菜单管理 | 切到菜品管理，展示编辑、多语言、图片、options | 菜单管理支持图片上传、多语言内容、售罄、下架和口味选项。 |
| 80-87s | POS 前台点单 | 切到前台点单，展示购物车、付款和打印区域 | POS 前台点单适合员工人工下单，可选择付款方式并打印小票。 |
| 87-90s | 结束字幕 | 停留最后画面 | 扫码点餐 · 后台管理 · POS 点单 · 多语言菜单 |

## 字幕文案连续版

1. 餐馆官网、扫码点餐、后台管理和 POS 点单，一套系统完成。
2. 首页展示品牌、营业信息和查看菜单入口，客户一打开就能看懂。
3. 公开菜单适合分享给顾客浏览，只展示菜品、价格、图片和售罄状态。
4. 前台支持英文和希腊语，方便本地顾客和游客使用。
5. 顾客扫桌码进入点餐页，系统识别当前桌台和用餐会话。
6. 顾客按分类浏览菜品，选择后可以加入购物车。
7. 如果菜品有口味选项，可以选择辣度、温度或特殊要求。
8. 购物车按桌台会话隔离，同桌多人点餐也能保持一致。
9. 顾客提交订单后，后台可以实时看到新订单。
10. 订单列表集中展示桌号、状态、明细和收款情况。
11. 确认收款和清桌入口清楚可见，演示时不需要点击危险操作。
12. 桌台二维码可以下载、打印和重生成，旧二维码会失效。
13. 菜单管理支持图片上传、多语言内容、售罄、下架和口味选项。
14. POS 前台点单适合员工人工下单，可选择付款方式并打印小票。
15. 扫码点餐 · 后台管理 · POS 点单 · 多语言菜单。

## 录制前准备

必须配置：

- `BASE_URL`：项目地址，例如本地 `http://127.0.0.1:5173`
- `ADMIN_EMAIL`：后台演示账号邮箱
- `ADMIN_PASSWORD`：后台演示账号密码
- `DEMO_TABLE_URL` 或 `DEMO_QR_TOKEN`：演示桌台地址或桌码 token
- `DEMO_ALLOW_ORDER_SUBMIT=true`：明确确认这是本地环境或测试桌码，允许脚本提交一笔演示订单

为了避免误向真实门店提交订单，脚本没有设置 `DEMO_ALLOW_ORDER_SUBMIT=true` 时会直接退出。

## 运行命令

如果项目已安装 `tsx`：

```powershell
pnpm tsx scripts/demo-record.ts
```

如果还没有安装 `tsx`，二选一：

```powershell
pnpm add -D tsx
pnpm tsx scripts/demo-record.ts
```

或临时运行：

```powershell
pnpm dlx tsx scripts/demo-record.ts
```

Windows PowerShell 示例：

```powershell
$env:BASE_URL="http://127.0.0.1:5173"
$env:DEMO_QR_TOKEN="your-test-table-token"
$env:ADMIN_EMAIL="demo@example.com"
$env:ADMIN_PASSWORD="your-demo-password"
$env:DEMO_ALLOW_ORDER_SUBMIT="true"
pnpm dlx tsx scripts/demo-record.ts
```

如果使用完整桌台地址：

```powershell
$env:DEMO_TABLE_URL="http://127.0.0.1:5173/table/your-test-table-token"
```

输出目录：

```text
demo-videos/
```

找不到元素时，debug 截图会保存到：

```text
demo-videos/debug/
```

## 转成 MP4

```powershell
ffmpeg -i demo-videos\wok-dragon-demo-你的文件名.webm -c:v libx264 -pix_fmt yuv420p -movflags +faststart -an demo-videos\wok-dragon-demo.mp4
```

如果想保留浏览器声音，可以去掉 `-an`。当前脚本本身不生成音频。

## 导入剪映

1. 把 `.webm` 或转好的 `.mp4` 导入剪映。
2. 使用“字幕文案连续版”逐句添加字幕。
3. 如需配音，直接把字幕文案作为中文配音稿。
4. 添加轻快背景音乐，音量保持较低。
5. 对后台账号、订单号、桌码或客户数据做必要打码。
6. 导出建议：1080p、30fps、H.264 MP4。

